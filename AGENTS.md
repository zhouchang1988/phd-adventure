# AGENTS.md - 开发指导原则

## 文档同步规则

### 1. 大纲驱动变更

**核心原则**：`谢凡的奇幻冒险2.0-游戏策划大纲.md` 是所有文档的源头。

当大纲发生改动时，必须同步更新以下文档：

| 文档 | 同步内容 |
|------|----------|
| `关卡详细设计.md` | 关卡机制、敌人配置、奖励系统 |
| `技术设计文档.md` | 技术实现方案、数据结构、接口设计 |
| `美术风格指南.md` | 视觉表现需求、资源规格 |
| `纯CSS实现方案.md` | 纯CSS视觉实现方案、场景、角色、特效 |

**触发条件**：
- 大纲中的关卡描述变更 → 更新关卡详细设计
- 大纲中的玩法机制变更 → 更新技术设计文档
- 大纲中的视觉需求变更 → 更新美术风格指南
- 大纲中的视觉需求变更 → 更新纯CSS实现方案

### 2. 三向同步原则

**文档、代码、测试必须保持一致**。

当任一部分发生改动时，必须检查并同步其他部分：

```
文档改动 → 检查代码实现 → 更新测试用例
代码改动 → 检查文档描述 → 更新测试用例
测试改动 → 检查代码实现 → 更新文档描述
```

### 3. 同步检查清单

在提交任何改动前，必须完成以下检查：

- [ ] 大纲是否为最新版本？
- [ ] 相关文档是否已同步更新？
- [ ] 代码实现是否与文档描述一致？
- [ ] 测试用例是否覆盖了文档中的所有场景？
- [ ] 是否有遗漏的联动改动？

### 4. 变更影响分析

每次改动前，必须进行影响分析：

```markdown
## 变更影响分析

**改动内容**：[描述改动]

**影响范围**：
- 文档：[列出受影响的文档]
- 代码：[列出受影响的代码模块]
- 测试：[列出需要更新的测试]

**同步计划**：
1. [步骤1]
2. [步骤2]
```

### 5. 版本控制

- 所有文档改动必须与代码改动在同一提交中
- 提交信息必须说明同步的文档范围
- 使用语义化版本标记重大文档变更

## 工作流程

### 开发新功能

1. 先更新大纲（如需要）
2. 更新相关设计文档
3. 实现代码
4. 编写/更新测试
5. 验证文档、代码、测试一致性

### 修复问题

1. 确认问题是否源于文档不一致
2. 同步更新文档和代码
3. 更新相关测试用例
4. 验证修复的一致性

## 工具支持

### 自动化检查

建议使用以下工具辅助同步检查：

- `pre-commit`：提交前检查文档引用
- `markdown-link-check`：验证文档链接有效性
- `test-coverage`：确保测试覆盖文档场景

### 文档模板

所有设计文档应遵循统一模板，便于追踪改动：

```markdown
# [文档标题]

## 变更历史

| 日期 | 版本 | 改动内容 | 关联文档 |
|------|------|----------|----------|
| YYYY-MM-DD | 1.0 | 初始版本 | 大纲 vX.Y |

## [内容章节]
```

## 责任矩阵

| 角色 | 职责 |
|------|------|
| 策划 | 维护大纲，审核文档一致性 |
| 程序 | 实现代码，更新技术文档 |
| 美术 | 更新美术指南，验证视觉表现 |
| 测试 | 更新测试用例，验证一致性 |

## 紧急处理

当发现文档不一致时：

1. **立即标记**：在相关文档中标注不一致处
2. **评估影响**：确定不一致的影响范围
3. **优先修复**：将文档同步作为高优先级任务
4. **根因分析**：找出不一致产生的原因，防止再次发生